export default {
  async fetch(request) {
    // Hard‑coded JSON for Matrix federation
    const body = '{ "m.server": "matrix.yourdomain.com:443" }';
    return new Response(body, {
      headers: {
        "content-type": "application/json",
        // Optional: cache at the edge and in browsers for 1 hour
        "cache-control": "public, max-age=3600"
      }
    });
  }
}
